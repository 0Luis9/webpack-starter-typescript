
import db from'./Firebase/Config';

const usuario = {
    nombre: 'Maria',
    activo: true,
    fechaNaci: 0,
 }
// Insert into usuarios...

//db.collection('usuarios')
//.add(usuario)
//.then( docRef => {
//    console.log( docRef )
//})
//.catch(e => console.log('error', e))